meta {
  name: create-userEntity
  type: http
  seq: 4
}

post {
  url: {{PROTOCOL}}://{{DOMAIN_URL}}/userEntity-app
  body: json
  auth: inherit
}

body:json {
  {
    "username" : "bla", 
    "email" : "a@a.p", 
    "value" : "none"
  }
}
